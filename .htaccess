# ============================================
# 🔒 SÉCURITÉ API ATLANTIS
# ============================================



# ============================================
# 🔑 TRANSMISSION DU HEADER AUTHORIZATION
# ============================================
# OVH/Apache ne transmet pas le header Authorization par défaut
# Ces règles forcent sa transmission au PHP

RewriteEngine On

# Transmettre le header Authorization au PHP
RewriteCond %{HTTP:Authorization} .+
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

# Alternative pour certaines configs Apache
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

# Activer la réécriture d'URL
RewriteEngine On

# Forcer HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Bloquer l'accès aux fichiers sensibles
<FilesMatch "^(database|init)\.php$">
    Require all denied
</FilesMatch>

# Bloquer l'accès au dossier config directement
RewriteRule ^config/ - [F,L]

# Headers de sécurité
Header always set X-Content-Type-Options "nosniff"
Header always set X-Frame-Options "DENY"
Header always set X-XSS-Protection "1; mode=block"

# Désactiver le listing des répertoires
Options -Indexes

# Protection contre les injections XSS
# Limiter les méthodes HTTP autorisées

